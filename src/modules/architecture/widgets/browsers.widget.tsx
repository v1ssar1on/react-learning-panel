import { PageHeader } from '@/shared/components/page-header'

export const BrowsersWidget = () => {
  return (
    <div>
      <PageHeader
        title="Supported Browsers"
        description="Learn which browsers are supported by Next.js."
      />
    </div>
  )
}
